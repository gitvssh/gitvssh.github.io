---
import { SITE_NAME, TRACKS } from '../lib/content';

const currentPath = Astro.url.pathname;
const isCurrent = (path: string) =>
  path === '/' ? currentPath === path : currentPath.startsWith(path);
---

<header class="site-header">
  <div class="site-header__inner">
    <a class="wordmark" href="/" aria-label={`${SITE_NAME} 홈`}>
      <span class="wordmark__mark" aria-hidden="true">3</span>
      <span>{SITE_NAME}</span>
    </a>

    <nav aria-label="주요 메뉴">
      <ul class="site-nav">
        {
          Object.entries(TRACKS).map(([key, track]) => (
            <li>
              <a
                href={`/tracks/${key}/`}
                aria-current={isCurrent(`/tracks/${key}/`) ? 'page' : undefined}
              >
                {track.label}
              </a>
            </li>
          ))
        }
        <li>
          <a href="/about/" aria-current={isCurrent('/about/') ? 'page' : undefined}>
            소개
          </a>
        </li>
      </ul>
    </nav>
  </div>
</header>
